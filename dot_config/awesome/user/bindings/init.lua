return {
    client = {
        key = require("user.bindings.client.key"),
        mouse = require("user.bindings.client.mouse"),
    },
    global = {
        key = require("user.bindings.global.key"),
        mouse = require("user.bindings.global.mouse"),
    },
    mod = require("user.bindings.mod"),
}

