return {
  client = require "user.signals.client",
  naughty = require "user.signals.naughty",
  ruled = require "user.signals.ruled",
  screen = require "user.signals.screen",
  tag = require "user.signals.tag",
}
